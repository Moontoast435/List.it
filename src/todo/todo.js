const todoItem = (title, description, dueDate, priority, isCompleted) => {
        // Todo properties
        return { 
            title, 
            description, 
            dueDate, 
            priority, 
            isCompleted 
        };
    }




export {todoItem};